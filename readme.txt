To run a project in development mode you have to:

1- npm install

Every time you want to work on this project you have to(and keep open):
2- npm run compile:sass -> will compile sass and update css after every modification
3- npm live-server -> will autoreload after every html/css update()